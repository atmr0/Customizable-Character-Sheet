{
  "dependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "expr-eval": "^2.0.2",
    "svelte": "^5.50.1",
    "vite": "^7.3.1"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:single": "npm run build && node ./scripts/singlefile.cjs"
  }
}
